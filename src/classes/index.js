//esta clase es solo para hacer las exportaciones, de manera a reducion las importaciones desde la clase donde son llamados.

import { Todo } from './todo.class';
import { TodoList } from './todo-list.class';

export{
    Todo,
    TodoList
}